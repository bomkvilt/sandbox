#include "cf.test.hpp"
#include "2500-Glob8F - ski accidents.hpp"


namespace PROBLEM_TEST
{
	bool CheckResult(std::vector<std::pair<int, int>> links, const std::vector<int>& removed)
	{
		auto std::unordered_map<int, std::array<2>>
	}
};


TEST(tests, PROBLEM_NAME)
{
	/*
	auto cases = std::vector<std::tuple<int, , int>>
	{
		  {1, , }
		, {2, , }
	};
	
	for (auto [N, v, exp] : cases)
	{
		auto res = PROBLEM_NAME(v);
		EXPECT_EQ(res, exp) << "N = " << N;
	}
	*/
}
