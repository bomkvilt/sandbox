#include "cf.test.hpp"
#include "{{file}}"



TEST(tests, PROBLEM_NAME)
{
	/*
	auto cases = std::vector<std::pair<, >>
	{
		  {, }
		, {, }
	};
	
	for (auto [w, exp] : cases)
	{
		auto res = TESTNAME(w);
		EXPECT_EQ(res, exp) << "w = " << w;
	}
	*/
}
